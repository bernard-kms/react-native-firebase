<?xml version="1.0" encoding="utf-8"?>
<manifest package="io.invertase.firebase"
  xmlns:android="http://schemas.android.com/apk/res/android">
  <application>
    <meta-data
      android:name="task_executor_maximum_pool_size"
      android:value="${firebaseJsonTaskExecutorMaximumPoolSize}" />
    <meta-data
      android:name="task_executor_keep_alive_seconds"
      android:value="${firebaseJsonTaskExecutorKeepAliveSeconds}" />
   </application>
</manifest>
